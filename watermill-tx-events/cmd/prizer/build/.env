# PUBSUB_EMULATOR_HOST must be set on the actual container
# (i.e., cannot use default values in server code)

GCP_PUBSUB_EVENT_TOPIC=lottery-concluded
GCP_PROJECT_ID=transactional-events
PUBSUB_EMULATOR_HOST=gcp:8085

CONFIGOR_VERBOSE_MODE=false
CONFIGOR_DEBUG_MODE=false

DB_NAME=watermill
DB_USER=watermill
DB_PASS=password
DB_PORT=5432
DB_HOST=db
